import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  wrapperMap: {
    flex: 1,
  },
  addButton: {
    position: 'absolute',
    bottom: 0,
    right: 10,
    padding: 10,
  },
});

export default styles;
